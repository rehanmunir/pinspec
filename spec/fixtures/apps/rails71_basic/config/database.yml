default: &default
  adapter: postgresql
  host: localhost
  username: <%= ENV["USER"] %>
  pool: 5

development:
  <<: *default
  database: pinspec_rails71_basic_development

test:
  <<: *default
  database: pinspec_rails71_basic_test
